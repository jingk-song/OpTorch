1. 仿真结果的检测应该变成瞬态检测，即output=torch.cat([output,sum([(x**2).mean(dim=0)[-1] for x in sim.detectors[det_index].detector_values()["E"]]).unsqueeze(dim=0)],dim=0)这句代码是计算的累计场强，那么在实际仿真时，应该记录确定时间点的瞬态场强分布吗？
2. 光源的设置，应该就是按照代码里那样设置，一个波长的脉冲
3. 是不是要对代码的输出结果做一个fft变换到频域上才能实现和Lumerical的对齐呢？如果需要变换的话，是不是只需要达到稳态后的数据呢？
